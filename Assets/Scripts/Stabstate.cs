using UnityEngine;

public class Stabstate : PlayerBaseState
{
    // This class represents the state of the player when they are in a "stab" state.
    private float stabDuration = 0.5f; // Duration of the stab state
    private float stabTimer = 0f; // Timer to track the duration of the stab state
    private bool isStabbing = false; // Flag to check if the player is currently stabbing
    private Player player; // Reference to the player object




    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    
    }
}
